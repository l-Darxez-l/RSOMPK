# Plik konfiguracyjny dla klienta aplikacji MPK

# Konfiguracja połączenia z serwerem systemowym
System.Proxy=SIP:tcp -h localhost -p 10000

# Ustawienia logowania - poziom śledzenia
#Ice.Trace.Network=2
#Ice.Trace.Protocol=1

# Nazwa klienta dla celów logowania i narzędzi administracyjnych
Ice.ProgramName=mpk_client

# Ustawienia sieciowe - czas oczekiwania na odpowiedź
Ice.Override.Timeout=3000

# Ustawienia lokalne
Client.Locale=en_US